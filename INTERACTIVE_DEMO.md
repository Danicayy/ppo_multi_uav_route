# 交互式演示使用指南

## 功能说明

`interactive_demo.py` 提供了一个交互式界面，允许用户：
1. 手动标定每个无人机的起点位置
2. 手动标定每个无人机的终点位置
3. 手动标定障碍物位置
4. 使用训练好的模型运行路径规划仿真

## 环境参数

根据当前模型配置：
- 环境大小: 50x50
- 无人机数量: 2架
- 障碍物数量: 3个
- 障碍物半径: 4.0
- 目标区域半径: 5.0

## 使用方法

### 方式1: 使用最新模型
```bash
python interactive_demo.py
```
程序会自动从 `models/` 目录中选择episode数最大的模型。

### 方式2: 指定模型
```bash
python interactive_demo.py models/ppo_uav_0_episode_124500.pth
```

## 操作步骤

### 1. 标定起点
- 窗口标题会提示"请标定无人机1的起点"
- 在地图上点击鼠标左键标定位置
- 完成后自动进入下一个无人机的起点标定
- 起点显示为绿色圆点，标记为 S1, S2

### 2. 标定终点
- 完成所有起点后，自动进入终点标定
- 窗口标题会提示"请标定无人机1的终点"
- 在地图上点击鼠标左键标定位置
- 终点显示为红色星号，并有红色半透明圆圈表示目标区域
- 标记为 T1, T2

### 3. 标定障碍物
- 完成所有终点后，自动进入障碍物标定
- 窗口标题会显示"请标定障碍物1的位置 (剩余: 3)"
- 在地图上点击鼠标左键标定位置
- 障碍物显示为黑色叉号，并有黑色半透明圆圈表示障碍区域
- 标记为 O1, O2, O3

### 4. 运行仿真
- 完成所有标定后，窗口标题会显示"标定完成！关闭窗口继续..."
- 关闭标定窗口
- 程序会自动加载模型并开始仿真
- 仿真窗口会实时显示：
  - 无人机轨迹（彩色线条）
  - 当前无人机位置（彩色圆点）
  - 起点、终点、障碍物
  - 当前步数

### 5. 查看结果
- 仿真结束后会显示统计信息：
  - 总步数
  - 每个UAV是否到达目标
  - 未到达目标的UAV距离目标的距离

## 示例输出

```
============================================================
多无人机路径规划交互式演示
============================================================
环境大小: 50.0x50.0
无人机数量: 2
障碍物数量: 3
============================================================

标定完成！
起点: [[10.5 10.8] [12.3 9.5]]
终点: [[40.2 40.5] [38.5 42.1]]
障碍物: [[25.0 25.0] [30.0 20.0] [20.0 30.0]]

正在加载训练好的模型...
  UAV 0 模型加载完成
  UAV 1 模型加载完成

开始运行仿真...

✓ 仿真成功完成！所有UAV在 156 步内到达目标

统计信息:
  UAV1: 已到达目标 ✓
  UAV2: 已到达目标 ✓
```

## 注意事项

1. **起点和终点距离**: 建议起点和终点之间保持一定距离（至少20单位），以便观察路径规划效果
2. **障碍物位置**: 障碍物最好放置在起点和终点之间，否则可能对路径影响不大
3. **边界**: 标定时注意位置不要太靠近边界（建议距离边界至少5个单位）
4. **模型性能**: 模型性能取决于训练程度，建议使用训练至少10万episode以上的模型

## 常见问题

### Q: 无人机未能到达目标？
A: 可能原因：
- 模型训练不充分，尝试使用更多episode训练的模型
- 标定的场景与训练场景差异太大
- 障碍物太密集或阻挡了路径

### Q: 无人机发生碰撞？
A: 这可能是正常的，模型仍在学习避障。使用更新的模型通常有更好的避障能力。

### Q: 如何选择合适的模型？
A: 建议使用 `models/` 目录下episode数最大的模型，训练越充分效果越好。

## 技术细节

- 实时更新频率: 每5步更新一次可视化（提高性能）
- 最大仿真步数: 200步（可在config.py中修改MAX_STEPS）
- 动作空间: 连续动作空间 [-0.5, 0.5] (加速度)
- 状态空间: 包含位置、速度、相对位置等信息
